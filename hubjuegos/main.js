// Importamos la estructura del archivo initTemplate que es un Util.
import { initControler, initTemplate } from "./src/utils";
import "./style.css";

// Ejecutamos la estructura.
initTemplate();
initControler();
